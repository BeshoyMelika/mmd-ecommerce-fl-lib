mutation UpdateProfile(
    $name: String,
    $mobile: String, 
    $isNotifiable: Boolean
){
    updateProfile(input: {
        name: $name, 
        mobile:$mobile,
        is_notifiable:$isNotifiable
    })
}